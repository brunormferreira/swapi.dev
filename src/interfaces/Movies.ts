export default interface MoviesInterface {
  characters?: string[];
  created?: string;
  director: string;
  episode_id: number;
  opening_crawl: string;
  producer: string;
  release_date: string;
  title: string;
}
